<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>연습문제 결과 페이지</title>
    <style>
      span {
        color: coral;
        font-weight: 700;
      }
      ul {
        list-style: none;
        padding: 0;
        margin: 0;
      }
      li {
        border: 1px solid #bbb;
        box-shadow: 0px 0px 5px #c8c8c8;
        padding: 0.5rem;
        margin: 0.5rem 0;
        border-radius: 10px;
      }

      /* p{
        color: userInfo.color;
        color: <%#=userInfo.color%>; //+ 둘 다 불가능! script로 적용할 수 있다
      } */
    </style>
  </head>
  <body>
    <!-- 실습 해답 -->
    <h1>결과 확인하기</h1>
    <h3><span><%=userInfo.name%></span>님 환영합니다.</h3>
    <ul>
      <li>성별: <%=userInfo.gender%></li>
      <li>
        생년월일: <%=userInfo.year%>/<%=userInfo.month%>/<%=userInfo.date%>
      </li>
      <!-- <li>생년월일: yyyy/mm/dd </li> -->
      <li>관심사: <%=userInfo.favorite%></li>
    </ul>

    <%if(addInfo){%>
    <hr />
    <h4>다른 태그 작성해보기</h4>
    <p>좋아하는 색깔: <span class="color"><%=userInfo.color%></span></p>
    <p>좋아하는 숫자: <%=userInfo.number%></p>
    <p>다짐 한마디: <%=userInfo.comment%></p>
    <%}%>

    <script>
      // const color = document.querySelector(".color");
      // color.style.color = userInfo.color; //+ 적용x, 개발자 도구를 보면 userInfo가 적용되지 않았기 때문
      // color.style.color = "<%#= userInfo.color%>"; //+ 적용x, 오류는 안나는데 여전히 안됨

      document.addEventListener("DOMContentLoaded", () => {
        const color = document.querySelector(".color");
        color.style.color = "<%= userInfo.color%>";
      }); //+ 이렇게 할 수 있긴 한데 이렇게 쓸 일은 없을 것이다
    </script>
  </body>
</html>
